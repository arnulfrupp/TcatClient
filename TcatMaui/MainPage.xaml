<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="TcatMaui.MainPage"
             Title="Home">

    <ScrollView>
        <VerticalStackLayout
            Spacing="25"
            Padding="30,0"
            VerticalOptions="Center">

            <Label 
                Text="Version 0.2.0.0" 
                HorizontalOptions="Center" />

            <Button
                x:Name="CounterBtn"
                Text="Click me"
                SemanticProperties.Hint="Counts the number of times you click"
                Clicked="OnCounterClicked"
                HorizontalOptions="Center" />
            
            <Button
                x:Name="AddOtDeviceBtn"
                Text="Add device to the network with OT certificate"
                SemanticProperties.Hint="Add uncommissioned device to the Thread network"
                Clicked="OnAddOtDeviceBtn_Clicked"
                HorizontalOptions="Center" />

            <Button
                x:Name="AddInvDeviceBtn"
                Text="Add device to the network with Inventronics certificate"
                SemanticProperties.Hint="Add uncommissioned device to the Thread network"
                Clicked="OnAddInvDeviceBtn_Clicked"
                HorizontalOptions="Center" />

        </VerticalStackLayout>
    </ScrollView>

</ContentPage>
